- name: conditions with play
  hosts: local
  connection: local
  vars:
    number: 6
  tasks:
  - name: print the number if less than 10
    ansible.builtin.debug:
      msg: "Given Number {{ number }} is less than 10 "
    when: number < 10